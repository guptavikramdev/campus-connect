import frontOffice from './front-office';
import attendance from './attendance';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [frontOffice, attendance]
};

export default menuItems;
